spring:
  application:
    name: fakecom
  datasource:
    username: "root"
    password: "root"
    url: "jdbc:mysql://localhost:3306/fakecomm" 
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate 
  flyway:
    enabled: true
    locations: classpath:db/migration
  
  
       
  
  
 
 
  
        
